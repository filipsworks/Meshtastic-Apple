//
//  FreeDV.swift
//  Meshtastic
//
//  The 700C codec2 upgrade (for ha-bridge-compatible voice) vendors the codec2
//  vocoder only; the FreeDV modem C sources were neutralised. FreeDV was never
//  used by the app, so this wrapper is intentionally empty. Restore from git
//  history (and re-add the FreeDV C sources) if FreeDV is needed later.
//

import Foundation
